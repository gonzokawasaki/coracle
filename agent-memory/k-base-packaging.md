---
name: k-base-packaging
description: "AppImage packaging — BUILT & verified end-to-end 2026-06-14 (boots, ingests, original-retention). Collector read-only-mount fix applied. Proactive first-run model-download overlay BUILT 2026-06-14. API auth token gate BUILT 2026-06-14. Node-18 EOL exit DONE 2026-06-14 (migrated to Node 22, packaged AppImage rebuilt + verified end-to-end, no problems). Remaining: icon, collector :8888 auth. See PACKAGING.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: 44f63927-d2bd-49c9-bbe7-89fcb83dce90
---

**AppImage (build 1, Linux). BUILT & verified end-to-end 2026-06-14.** `yarn dist` → `amadocs-desktop/dist/AMAdocs-0.1.0-x86_64.AppImage` (~562 MB; squashfs of a 2.6 GB tree). Step map in repo `PACKAGING.md`.

**Decisions:** bundle **Node 18** + **Ollama** binaries (no models → first-run download). AppImage mount is **read-only** → all writable state relocated to `userData` (`~/.config/AMAdocs/`); read-only model assets (51 MB embedder/OCR/reranker) + a clean pre-migrated **empty** DB seeded into userData on first run.

**The 4 prep steps (all DONE 2026-06-14):**
1. ✅ Prisma DB path env-driven — `schema.prisma` now `url = env("DATABASE_URL")`; added `DATABASE_URL` to `server/.env.development` (dev identical); `prisma generate`. Verified: dev DB connects, packaged DB points at userData.
2. ✅ Clean seed DB `vendor/anythingllm.db` via `migrate deploy` — **ships EMPTY** (0 workspaces/0 docs; verified — no stray user data).
3. ✅ Staged `vendor/node/node` (**Node 22** — migrated off 18, see below) + `vendor/ollama/ollama` (0.30.7); `+x` preserved in AppImage.
4. ✅ Model assets bundled straight from `server/storage/models` (per package.json extraResources `storage-seed/models`) — no separate staging.

**Verified live in the packaged app (2026-06-14, GTX 1650 Ti) — FULL STACK now works:** boots from the AppImage mount; first-run userData seeding (models, per-install secrets, empty DB); server :3001 + collector :8888 + ollama up; **doc ingest** (parse → native embed → lance table → original retained), **vision captioning** (moondream → `caption: yes`), and **chat** (phi3.5 answered grounded + cited source) all verified end-to-end. Fresh workspaces embed text+images fine.

**⚠️ CRITICAL packaging fix — Ollama runtime libs (found+fixed 2026-06-14):** the first builds bundled ONLY `vendor/ollama/ollama` (38 MB binary). Modern Ollama (0.30.7) runs inference via a **separate `llama-server` + ~2.1 GB of runner libs in `lib/ollama/`** (cuda_v12 1.2 G, cuda_v13 807 M, vulkan 56 M, CPU core 27 M). Without them, EVERY model 404s ("error starting llama-server: llama-server binary not found") — chat AND vision both dead; only native-ONNX embedding worked, which masked it. Fix: bundle the whole dev `tooling/ollama/` (bin/ + lib/) → `vendor/ollama`, mapped to `resources/ollama/` (extraResources `{from:vendor/ollama,to:ollama}` unchanged); `main.js OLLAMA_BIN` packaged path → `resources/ollama/bin/ollama` (ollama auto-finds `../lib/ollama`). electron-builder PRESERVES the lib symlinks (verified — not dereferenced). AppImage 562 MB → **2.3 GB** (user chose full bundle: both CUDA majors + vulkan + CPU, works on any NVIDIA driver / CPU fallback; pruning to one CUDA is a later size win). **Don't ever stage just the ollama binary again — it needs lib/ollama.**

**Friendly model-missing prompt — BUILT 2026-06-14 (user chose "friendly prompt, manual download" over auto-download or just-error):** chat used to leak the raw engine error (`Sorry — Model router error: model 'phi3.5' not found`). UI (`tooling/amadocs-ui/index.html` → synced to desktop) now detects model-missing errors in the `ask()` abort handler via `isModelMissingError()` (regex: `\bmodel\b` + not-found/not-installed/try-pulling/404; ignores ECONNREFUSED=engine-down) and renders an in-bubble `.modelmiss` prompt with a "Download your AI…" button → `openDownloadModal()` (the existing catalog/pull-model SSE modal). Regex validated against the real logged string `model 'phi3.5' not found`. **Not yet eyeballed live in-UI** (needs a missing-model + human eyeball — standard open item). Chat Q&A NOT possible until a chat model is pulled — bundle ships none.

**Collector read-only-mount fix (found+fixed 2026-06-14 — NOT in PACKAGING.md originally):** first packaged run crashed the collector — `wipeCollectorStorage` (`collector/utils/files/index.js`) read a non-existent `hotdir` and fell through to iterate `undefined` (`for (const file of files)` after `if(err) resolve()` with no `return`). Deeper issue: `hotdir`/`storage/tmp` resolved relative to the read-only mount, and server multer wrote uploads to a *different* path (`STORAGE_DIR/../../collector/hotdir`) than the collector read — so even past the crash, uploads wouldn't be found. Fix (all tagged `AMAdocs:`): `collector/utils/constants.js` now exports `WATCH_DIRECTORY` + new `TMP_DIRECTORY`, both `NODE_ENV`-gated → packaged points to `STORAGE_DIR/hotdir` & `STORAGE_DIR/tmp` (dev unchanged); `wipeCollectorStorage` uses those + `return resolve()` on readdir error; `localWhisper.js` uses `TMP_DIRECTORY`; server `utils/files/multer.js` both hotdir dests → `STORAGE_DIR/hotdir` (matches collector); `main.js ensurePackagedStorage` creates `hotdir`+`tmp` (multer won't). Rebuilt + re-verified.

**Proactive first-run model download — BUILT 2026-06-14.** On boot, `maybeFirstRun()` (`tooling/amadocs-ui/index.html` → synced to desktop) calls `custom-models`; if it answers with NO installed *chat* model (hidden/vision filtered; a network error is treated as "engine warming up", NOT first-run, so no false nag), it shows a full-screen `#firstRun` "Welcome to AMAdocs" overlay. One button pulls the default AI (phi3.5) with a live progress bar; an opt-in (checked-by-default) "Let AMAdocs read images & scans" pulls moondream afterwards — both over the existing `pull-model` SSE. Sizes from the catalog (best-effort + fallbacks). Success → `loadCurrentModel()` + auto-dismiss; failure → "Try again"; "Set up later" → dismiss, reactive prompt still covers them. The SSE-reading loop is now a shared `streamModelPull(id,onProgress)` helper used by BOTH the first-run screen and the download modal (refactored out of `pullModel`). Static/syntax-verified + UI copies synced; **live overlay render + a true model-less-install download still want a human eyeball** (couldn't trigger live — dev Ollama already has models). Still unbuilt: a status bubble when a *feature* (vision) needs a model the user skipped.

**Distribution direction (decided 2026-06-14): Linux-first ALPHA.** Channels = AppImage + electron-builder **`pacman`** target (added to `amadocs-desktop/package.json` `build.linux.target`: `["AppImage","pacman"]`, `artifactName` → `${ext}`, `maintainer:"AMAdocs"` — pacman/fpm may want a `Name <email>` maintainer; confirm an identity/email before a real pacman build) + **AUR** (a `-bin` PKGBUILD that downloads a released artifact + checksum — so **AUR is gated on a public release host**, ties to the parked GitHub publish [[k-base-github-staging]]; from-source AUR isn't realistic for the Electron+engine+Ollama stack). **Mac deferred** to ~later that week; MIT so the community can fork/build it. Rationale (user): Linux users tolerate alpha rough edges + it dodges the macOS signing/notarization/$99-cert gauntlet. **Licensing rule:** LICENSE (MIT) + THIRD_PARTY_LICENSES must **accompany any PUBLIC binary** (MIT/Apache attribution is a *distribution*-triggered obligation) → required before a public release, NOT for purely-local test builds; an in-app About/Licenses screen is later polish, a shipped text file satisfies the obligation.

**Licensing — DONE 2026-06-14.** Root `LICENSE` (MIT) + generated `THIRD_PARTY_LICENSES` (Part A curated bundled components, Part B runtime-downloaded models, Part C = 1628 unique npm pkgs with full notices). Generator: `node tooling/gen-third-party-licenses.js` (walks the 3 node_modules trees, dedupes name@version, embeds curated header `tooling/_tpl-header.txt`). **Audited clean:** all permissive (MIT 1118/Apache 297/ISC/BSD/BlueOak), NO GPL/AGPL; dual-licenses elected permissive (jszip→MIT, mailsplit→MIT, dompurify→Apache); LGPL `libvips`(via sharp) + MPL `web-push`/`dompurify` noted as permitted-with-attribution; 3 undeclared legacy pkgs (buffers/combine-errors/voyageai) attributed by author convention; cohere-ai/voyageai/milvus are unused upstream provider SDKs (prune candidate + size win). Both files ship via electron-builder `extraResources`. **Polish TODO:** verbatim Ollama+llama.cpp upstream LICENSE files into `vendor/ollama/` (currently MIT terms reproduced+sourced, not the files); in-app About→Licenses screen (text file already satisfies the obligation). Rule recap: licensing required to accompany PUBLIC binaries, not local test builds.

**API auth token gate — BUILT 2026-06-14.** Closed the open-localhost :3001 hole (Fable review #3). `main.js` mints a per-boot secret (`crypto.randomBytes(32)`, module-level `API_TOKEN`, NOT persisted), passes it to the engine as `AMADOCS_API_TOKEN` (in `packagedEngineEnv`) and to the renderer via `webPreferences.additionalArguments=["--amadocs-api-token=…"]` → `preload.js` reads argv → `window.amadocs.apiToken`. Engine (`server/index.js`) gates EVERY `/api` request: middleware on `apiRouter` mounted before the route handlers, timing-safe compare (`crypto.timingSafeEqual`), accepts `Authorization: Bearer <t>` OR `?token=<t>` (the latter for the `doc-export` download anchor, which can't send a header), `OPTIONS` preflight passes through. **Token unset => passthrough**, so the dev/browser stack is unchanged (mirrors AnythingLLM's `validatedRequest` dev-passthrough). UI auths via a one-line `window.fetch` shim that adds the Bearer header to every `${API}` call (zero changes to ~20 call sites) + `apiUrl()` on the export anchor; synced to both UI copies. `/api/ping` returning 401 still counts as "up" for `main.js` boot polling (checks `<500`). **Verified:** 6-case standalone gate test (no-token/wrong-bearer/wrong-?token→401, valid bearer/valid ?token/OPTIONS→200) all pass; 3 JS files syntax-clean; UI copies identical. **Not yet eyeballed:** live packaged launch (same human-eyeball class as other packaged items). **Remaining:** collector :8888 is still unauthenticated (server→collector internal, UI never calls it directly → lower risk; defense-in-depth follow-up).

**Node-18 EOL exit — DONE 2026-06-14 (migrated to Node 22, packaged AppImage rebuilt + verified end-to-end, NO problems).** No native-module rebuild was needed — the "native modules don't build on Node 26" lore is about *source* builds; every native module the engine ships is a **prebuilt N-API binary** (ABI-stable across Node majors): `@lancedb/lancedb` 0.15, `sharp` 0.32.6, `canvas` 3.2.1, `onnxruntime-node` 1.14, `@prisma/client` 5.3.1, all `require()` + run unchanged. We staged a **Node 22** binary into `vendor/node/node`, bumped the `.nvmrc`s, and **rebuilt the packaged AppImage**; a full boot → ingest → ONNX embed → lancedb retrieval → grounded phi3.5 chat passed end-to-end on the packaged Node 22 app. Node 24 (current Active LTS) also runs unchanged on the dev stack; **22 chosen as the more conservative LTS jump.** `engines` is only `>=18` (no upper bound). `ELECTRON_RUN_AS_NODE` fallback wasn't needed.

**⚠️ The on-disk `dist/` AppImage is STALE — verified live 2026-06-14 PM.** It was built ~06:54, BEFORE that afternoon's source work. A live run of the actual bundle proved the gap: `window.amadocs` has no `apiToken`, the API answers HTTP 200 with no token (gate in passthrough — `AMADOCS_API_TOKEN` never set in the bundle), and the bundled engine's `workspaces.js` has **0** `doc-summarize`/`doc-export-embedded` routes. So the shipped artifact is MISSING: the API-token gate, the right-click summariser (UI+endpoints), embed-summary, the phi3.5 leak guard, and the current UI — **all present in source**, none in the bundle. **One `yarn dist` ships everything at once; do that before any release.** Conversely the headline **citation loop was EYEBALLED LIVE and works** (chip → PDF → pixel-accurate passage highlight; screenshots `tooling/logs/live-*.png`) — closes the #1 standing eyeball item. The dev stack is the source of truth until rebuilt.

**Still-open shipping blockers** (Fable, [[k-base-product-direction]]): **rebuild the AppImage** (see staleness note above — folds in ALL the afternoon work), app icon (default Electron icon used), collector :8888 auth (lower-risk follow-up). (Licensing files ✅ done above; pacman target added to package.json; AppImage still needs a rebuild to fold in the first-run screen + license files.) NOT a blocker anymore: CPU perf — [[k-base-gpu-app-decision]] dropped the CPU-only claim (GPU app). AI Finder off-roadmap ([[k-base-folder-index]]). Cross-platform (win/mac) needs those OSes/CI.
